spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/test
    username: test
    password: test
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
  flyway:
    enabled: true

language:
  base-url: http://localhost:8082

management:
  endpoints:
    web:
      exposure:
        include: health,info
